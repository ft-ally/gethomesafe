To do:

Click on map to add markers (saved loc)
2. Search for places
3. Calculate routes between points
4. Save/load locations from localStorage
5. Add UI controls (buttons, search bar)

Search
		Typing in search shows results
		Search adds destination/location

Markers
DONE 		adding the first pin as origin
DONE		adding the origin as first pin
DONE		adding destination as pin
			adding new search bar when there are more pins
			adding pin in destination
DONE		geotag saves as origin
DONE		geotag shows address on search bar


Routing
API call
Take me there button reloads sidebar



Buttons:
		clicking on add button shows a new search bar
DONE		clicking on location button checks geolocation